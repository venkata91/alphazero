# Chess AZ refinement config for the repaired pretraining run.
# Starts from pretrained_fixed.pt and writes fresh refinement outputs.

n_blocks = 15
n_channels = 192

num_simulations = 200
c_puct = 2.5
dirichlet_alpha = 0.3
dirichlet_weight = 0.25

games_per_iteration = 100
temperature_threshold = 30

training_steps_per_iteration = 2000
batch_size = 256
learning_rate = 1e-4
weight_decay = 1e-4

replay_buffer_capacity = 200_000
min_buffer_size = 10_000

eval_interval = 2
eval_games = 25

num_iterations = 10

num_workers = 4
inference_batch_size = 64
heartbeat_timeout_s = 300.0
heartbeat_interval_s = 5.0
progress_log_interval_s = 60.0

seed = 42
device = "auto"
checkpoint_dir = "checkpoints_refine_fixed"
log_dir = "runs_refine_fixed"
